(only thing generated by ai I PROMISE)
Phase 1: The Core Loop (Branch: feature/core-engine)

    [ ] Define the Model & Puzzle State: Create the model struct with a 2D slice for the board and a matching "solution" grid.

    [ ] Implement Navigation: Handle tea.KeyMsg for arrows/WASD and bounds checking (preventing the cursor from leaving the grid).

    [ ] Toggle Logic: Add keybindings (e.g., Enter or Space to fill, X to mark) that update the state in the Update function.

Phase 2: The View & UI (Branch: feature/tui-styling)

    [ ] Build the Board View: Use lipgloss to render the grid with borders.

    [ ] Dynamic Centering: Implement tea.WindowSizeMsg to capture terminal dimensions so lipgloss.Place always keeps the board centered.

    [ ] Cursor Styling: Use lipgloss.Style to make the current cell visually distinct (e.g., a different background color).

Phase 3: The Puzzle Logic (Branch: feature/game-rules)

    [ ] Clue Generation: Write a function that calculates the numbers (sequences of filled cells) for each row and column based on the solution grid.

    [ ] Render Clues: Update the View to display these numbers on the top and left sides of the board.

    [ ] Win Condition: Implement a check in the Update loop to compare the current board state against the solution and trigger a "Victory" state.

Phase 4: Content & UX (Branch: feature/polish)

    [ ] Level Loading: Allow the game to load puzzle layouts from a simple JSON or YAML file.

    [ ] Instructions Overlay: Add a "Help" menu or footer (e.g., q: quit | space: fill | x: mark).

    [ ] README & Documentation: Clear instructions on how to go run main.go.

